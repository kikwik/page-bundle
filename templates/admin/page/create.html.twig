{% extends '@KikwikPage/admin/layout.htlm.twig' %}

{% block body %}
    <div class="card">
        <div class="card-header">
            Crea {% if parent %}pagina figlia di {{ parent }}{% else %}homepage{% endif %}
        </div>
        <div class="card-body">
            {% include '@KikwikPage/admin/page/_form.html.twig' %}
        </div>
    </div>

{% endblock %}
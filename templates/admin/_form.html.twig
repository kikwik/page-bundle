{% form_theme form 'bootstrap_5_horizontal_layout.html.twig' %}

{{ form_start(form) }}
    {{ form_widget(form) }}
    <a href="{{ path('kikwik_page_admin_list') }}" class="btn btn-outline-secondary">Annulla</a>
    <button type="submit" class="btn btn-outline-primary">Salva</button>
{{ form_end(form) }}